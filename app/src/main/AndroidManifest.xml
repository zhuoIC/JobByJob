<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nico.jobbyjob">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_jobbyjob"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".AboutAsActivity" />
        <activity android:name=".login.LoginViewImpl" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".prefs.GeneralSettingActivity"/>
        <activity android:name=".prefs.AccountSettingActivity"/>
    </application>

</manifest>